import { createWelcomeEmail } from './shared.js';
export const wiseOwlsTuesdaySeriesWelcome = createWelcomeEmail({ program: 'Wise Owls — Tuesday series', closing: 'We’re looking forward to walking the trails together.' });
